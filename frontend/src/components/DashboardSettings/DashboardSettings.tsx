import styles from "./DashboardSettings.module.scss";

const DashboardSettings: React.FC = () => {
  return <section className={styles.settingsBlock}>huy</section>;
};

export default DashboardSettings;
